# Paper tables

LaTeX tables, Figure 1 and `paper_facts.json` exactly as used in the paper.
Regenerate with `python tools/paper_tables.py --out <dir>` from `Code/Code_Phase_3`;
every value is read from `results/outputs/` (registry, paper_numbers.json).
