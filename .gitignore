# ── Secrets — NEVER commit ──────────────────────────────────
.env

# ── Python ──────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg
.eggs/

# ── Virtual environments ─────────────────────────────────────
venv/
.venv/
env/
~/venv_debate_study/

# ── Data (large files — regenerated by pipeline) ─────────────
data/raw/
data/processed/
data/outputs/

# ── Logs ─────────────────────────────────────────────────────
/logs/

# ── OS / editor ──────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
*.code-workspace

# ── HuggingFace cache ────────────────────────────────────────
*.cache/

# ── LaTeX build artefacts ────────────────────────────────────
Submission/.build/
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.synctex.gz
*.toc
# ── Paper drafts — keep OFF GitHub (private) ──
Submission/

results/logs/

# Operational notes for the author — never part of the anonymised artefact
# (they name the real repository, balances, and submission strategy).
ACTION_REQUIRED.md
Recharge.md

# Frozen pre-audit copies of the private manuscript
audit/baseline/
audit/ai_assistance_log.md

# LaTeX build logs at repo root contain absolute local paths.
*.log
